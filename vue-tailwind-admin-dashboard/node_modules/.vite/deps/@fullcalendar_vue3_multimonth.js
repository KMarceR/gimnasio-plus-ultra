import {
  DayGridHeaderRow,
  DayGridLayoutPrint,
  DayGridRows,
  DayTableSlicer,
  RefMap,
  Ruler,
  TableDateProfileGenerator,
  buildDateRowConfig,
  buildDayTableModel,
  buildDayTableRenderRange,
  createDayHeaderFormatter,
  dayMicroWidth
} from "./chunk-OIPELTEC.js";
import "./chunk-WHYN7QHB.js";
import {
  DateComponent,
  NowTimer,
  Scroller,
  ViewContainer,
  afterSize,
  buildNavLinkAttrs,
  generateClassName,
  getIsHeightAuto,
  memoize,
  setRef,
  watchHeight
} from "./chunk-5M2F6UP2.js";
import {
  classNames,
  computeViewBorderless,
  createDuration,
  createFormatter,
  formatIsoMonthStr,
  intersectRanges,
  joinDateTimeFormatParts
} from "./chunk-Y4BOYFG5.js";
import {
  M,
  k,
  u
} from "./chunk-3AI4KBXI.js";
import {
  fracToCssDim,
  joinClassNames
} from "./chunk-NMFWHO23.js";
import "./chunk-PZ5AY32C.js";

// node_modules/@fullcalendar/vue3/chunks/325dec79.js
var SingleMonth = class extends DateComponent {
  constructor() {
    super(...arguments);
    this.state = {};
    this.buildDayTableModel = memoize(buildDayTableModel);
    this.createDayHeaderFormatter = memoize(createDayHeaderFormatter);
    this.buildDateRowConfig = memoize(buildDateRowConfig);
    this.titleElRef = M();
    this.rowHeightRefMap = new RefMap(() => {
      afterSize(this.handleHeights);
    });
    this.slicer = new DayTableSlicer();
    this.handleEl = (el) => {
      var _a;
      const { options } = this.context;
      if (el) {
        this.rootEl = el;
        (_a = options.singleMonthDidMount) == null ? void 0 : _a.call(options, {
          el: this.rootEl,
          ...this.renderProps
        });
      }
    };
    this.handleTableHeaderEl = (el) => {
      var _a;
      (_a = this.disconnectTableHeaderHeight) == null ? void 0 : _a.call(this);
      this.disconnectTableHeaderHeight = void 0;
      if (el) {
        this.disconnectTableHeaderHeight = watchHeight(el, (height) => {
          this.setState({ tableHeaderHeight: this.tableHeaderHeight = height });
          afterSize(this.handleHeights);
        });
      }
    };
    this.handleGridWidth = (gridWidth) => {
      if (this._isUnmounting)
        return;
      this.setState({ gridWidth });
    };
    this.handleHeights = () => {
      if (this._isUnmounting)
        return;
      setRef(this.props.heightsRef, {
        titleHeight: this.titleHeight,
        tableHeaderHeight: this.tableHeaderHeight,
        rowHeightMap: this.rowHeightRefMap.current,
        cellRows: this.cellRows
      });
    };
  }
  get titleId() {
    return this.context.baseId + "month-" + this.props.isoDateStr;
  }
  render() {
    const { props, state, context } = this;
    const { dateProfile, forPrint } = props;
    const { options, dateEnv } = context;
    const { borderlessX, borderlessTop, borderlessBottom } = computeViewBorderless(options);
    const dayTableModel = this.buildDayTableModel(dateProfile, context.dateProfileGenerator, dateEnv);
    const slicedProps = this.slicer.sliceProps(props, dateProfile, options.nextDayThreshold, context, dayTableModel);
    const dayHeaderFormat = this.createDayHeaderFormatter(
      options.dayHeaderFormat,
      false,
      // datesRepDistinctDays
      dayTableModel.colCount
    );
    const rowConfig = this.buildDateRowConfig(
      dayTableModel.headerDates,
      false,
      // datesRepDistinctDays
      dateProfile,
      props.todayRange,
      dayHeaderFormat,
      context
    );
    this.cellRows = dayTableModel.cellRows;
    const isTitleAndHeaderSticky = !forPrint && props.colCount === 1;
    const isAspectRatio = !forPrint || props.hasLateralSiblings;
    const cellColCnt = dayTableModel.cellRows[0].length;
    const measuredColWidth = state.gridWidth != null ? state.gridWidth / cellColCnt : void 0;
    const cellIsMicro = measuredColWidth != null && measuredColWidth <= dayMicroWidth;
    const cellIsNarrow = cellIsMicro || measuredColWidth != null && measuredColWidth <= options.dayNarrowWidth;
    const rowHeightGuess = state.gridWidth != null ? 1 / options.aspectRatio * state.gridWidth / 6 : void 0;
    const headerStickyBottom = isTitleAndHeaderSticky ? rowHeightGuess : void 0;
    const titleStickyBottom = isTitleAndHeaderSticky && rowHeightGuess != null && state.tableHeaderHeight != null ? rowHeightGuess + state.tableHeaderHeight + 1 : void 0;
    const businessHourSegs = slicedProps.businessHourSegs;
    const dateSelectionSegs = forPrint ? [] : slicedProps.dateSelectionSegs;
    const eventDrag = forPrint ? null : slicedProps.eventDrag;
    const eventResize = forPrint ? null : slicedProps.eventResize;
    const tableMode = forPrint && !props.hasLateralSiblings;
    const hasNavLink = options.navLinks && props.colCount > 1;
    const headerRenderProps = {
      multiMonthColumns: props.colCount || 0,
      isSticky: isTitleAndHeaderSticky,
      isNarrow: cellIsNarrow,
      hasNavLink
    };
    const monthStartDate = props.dateProfile.currentRange.start;
    const navLinkAttrs = hasNavLink ? buildNavLinkAttrs(context, monthStartDate, "month", props.isoDateStr) : {};
    return u("div", { role: "listitem", style: { width: props.width }, children: u("div", { role: "grid", "aria-labelledby": this.titleId, "data-date": props.isoDateStr, className: joinClassNames(generateClassName(options.singleMonthClass, {
      isFirst: props.isFirst,
      isLast: props.isLast,
      multiMonthColumns: props.colCount || 0
    }), classNames.flexCol, props.hasLateralSiblings && classNames.breakInsideAvoid), children: [u(Ruler, { widthRef: this.handleGridWidth }), u("div", { id: this.titleId, ref: this.titleElRef, className: joinClassNames(generateClassName(options.singleMonthHeaderClass, headerRenderProps), isTitleAndHeaderSticky && classNames.stickyT, isTitleAndHeaderSticky && classNames.z3, classNames.flexCol), style: {
      // HACK to keep zIndex above table-header,
      // because in Chrome, something about position:sticky on this title div
      // causes its bottom border to no be considered part of its mass,
      // and would get overlapped and hidden by the table-header div
      marginBottom: titleStickyBottom
    }, children: u("div", { ...navLinkAttrs, className: joinClassNames(generateClassName(options.singleMonthHeaderInnerClass, headerRenderProps), navLinkAttrs.className), children: joinDateTimeFormatParts(dateEnv.formatToParts(monthStartDate, props.titleFormat)) }) }), tableMode ? u(DayGridLayoutPrint, { dateProfile: props.dateProfile, todayRange: props.todayRange, cellRows: dayTableModel.cellRows, headerTiers: [rowConfig], showHeader: true, headerElRef: this.handleTableHeaderEl, fgEventSegs: slicedProps.fgEventSegs, bgEventSegs: slicedProps.bgEventSegs, businessHourSegs, eventSelection: slicedProps.eventSelection, dayMaxEventRows: true, borderlessX, borderlessTop, borderlessBottom, multiMonthColumns: props.colCount || 0, visibleWidth: state.gridWidth, cellIsNarrow, cellIsMicro, rowHeightRefMap: this.rowHeightRefMap, style: {
      marginTop: titleStickyBottom != null ? -titleStickyBottom : void 0
    } }) : u("div", { className: joinClassNames(generateClassName(options.tableClass, {
      borderlessX,
      borderlessTop,
      borderlessBottom,
      multiMonthColumns: props.colCount || 0
    }), classNames.flexCol), style: {
      marginTop: titleStickyBottom != null ? -titleStickyBottom : void 0
    }, children: [u("div", { ref: this.handleTableHeaderEl, className: joinClassNames(generateClassName(options.tableHeaderClass, {
      isSticky: isTitleAndHeaderSticky,
      borderlessX,
      borderlessTop,
      borderlessBottom,
      multiMonthColumns: props.colCount || 0
    }), classNames.flexCol, isTitleAndHeaderSticky && classNames.sticky, isTitleAndHeaderSticky && classNames.z2), style: {
      top: isTitleAndHeaderSticky ? state.titleHeight : 0,
      marginBottom: headerStickyBottom
    }, children: [u(DayGridHeaderRow, { ...rowConfig, role: "row", borderBottom: false, cellIsNarrow, cellIsMicro, rowLevel: 0 }), u("div", { className: generateClassName(options.dayHeaderDividerClass, {
      isSticky: isTitleAndHeaderSticky,
      multiMonthColumns: props.colCount || 0,
      options: { allDaySlot: Boolean(options.allDaySlot) }
    }) })] }), u("div", { className: joinClassNames(generateClassName(options.tableBodyClass, {
      borderlessX,
      borderlessTop,
      borderlessBottom,
      multiMonthColumns: props.colCount || 0
    }), classNames.flexCol, isAspectRatio && classNames.rel, isTitleAndHeaderSticky && classNames.z1), style: {
      marginTop: headerStickyBottom != null ? -headerStickyBottom : void 0,
      aspectRatio: isAspectRatio ? String(options.aspectRatio) : void 0
    }, children: u(DayGridRows, {
      dateProfile: props.dateProfile,
      todayRange: props.todayRange,
      cellRows: dayTableModel.cellRows,
      className: isAspectRatio ? classNames.fill : "",
      forPrint: forPrint && !props.hasLateralSiblings,
      dayMaxEventRows: forPrint && props.hasLateralSiblings ? 1 : true,
      // content
      fgEventSegs: slicedProps.fgEventSegs,
      bgEventSegs: slicedProps.bgEventSegs,
      businessHourSegs,
      dateSelectionSegs,
      eventDrag,
      eventResize,
      eventSelection: slicedProps.eventSelection,
      // dimensions
      visibleWidth: state.gridWidth,
      cellIsNarrow,
      cellIsMicro,
      rowHeightRefMap: this.rowHeightRefMap
    }) })] })] }) });
  }
  componentDidMount() {
    this._isUnmounting = false;
    this.disconnectTitleHeight = watchHeight(this.titleElRef.current, (height) => {
      this.setState({ titleHeight: this.titleHeight = height });
      afterSize(this.handleHeights);
    });
  }
  componentWillUnmount() {
    var _a, _b;
    const { options } = this.context;
    this._isUnmounting = true;
    this.disconnectTitleHeight();
    (_a = this.disconnectTableHeaderHeight) == null ? void 0 : _a.call(this);
    (_b = options.singleMonthWillUnmount) == null ? void 0 : _b.call(options, {
      el: this.rootEl,
      ...this.renderProps
    });
  }
};
var MultiMonthView = class extends DateComponent {
  constructor() {
    super(...arguments);
    this.state = {};
    this.splitDateProfileByMonth = memoize(splitDateProfileByMonth);
    this.buildMonthFormat = memoize(buildMonthFormat);
    this.scrollerRef = M();
    this.tilesElRef = M();
    this.scrollState = {};
    this.handleInnerWidth = (innerWidth) => {
      if (this._isUnmounting)
        return;
      this.setState({ innerWidth });
    };
    this.handleScrollStart = () => {
      this.scrollState.date = void 0;
      this.scrollState.top = void 0;
    };
    this.handleScrollEnd = (isDevice) => {
      const scroller = this.scrollerRef.current;
      if (isDevice && scroller) {
        this.scrollState.top = scroller.y;
        this.scrollState.date = void 0;
      }
    };
  }
  render() {
    const { context, props, state } = this;
    const { options } = context;
    const verticalScrolling = !props.forPrint && !getIsHeightAuto(options);
    const monthDateProfiles = this.splitDateProfileByMonth(context.dateProfileGenerator, props.dateProfile, context.dateEnv, options.fixedWeekCount, options.showNonCurrentDates);
    const monthTitleFormat = this.buildMonthFormat(options.singleMonthTitleFormat, monthDateProfiles);
    const { multiMonthMaxColumns, singleMonthMinWidth } = options;
    const { innerWidth } = state;
    let cols;
    let cssMonthWidth;
    let hasLateralSiblings = false;
    if (innerWidth != null) {
      cols = Math.max(1, Math.min(multiMonthMaxColumns, Math.floor(innerWidth / singleMonthMinWidth)));
      if (props.forPrint) {
        cols = Math.min(cols, 2);
      }
      cssMonthWidth = fracToCssDim(1 / cols);
      hasLateralSiblings = cols > 1;
    }
    return u(NowTimer, { unit: "day", children: (nowDate, todayRange) => u(ViewContainer, { viewSpec: context.viewSpec, className: joinClassNames(
      // HACK for Safari. Can't do break-inside:avoid with flexbox items, likely b/c it's not standard:
      // https://stackoverflow.com/a/60256345
      !props.forPrint && classNames.flexCol,
      props.className
    ), children: [u(Scroller, { vertical: verticalScrolling, className: verticalScrolling ? classNames.liquid : "", ref: this.scrollerRef, children: u("div", { role: "list", ref: this.tilesElRef, "aria-labelledby": props.labelId, "aria-label": props.labelStr, className: classNames.safeTiles, children: monthDateProfiles.map((monthDateProfile, i) => {
      const monthStr = formatIsoMonthStr(monthDateProfile.currentRange.start);
      return k(SingleMonth, { ...props, key: monthStr, todayRange, isoDateStr: monthStr, titleFormat: monthTitleFormat, dateProfile: monthDateProfile, width: cssMonthWidth, colCount: cols, isFirst: !i, isLast: i === monthDateProfiles.length - 1, hasLateralSiblings });
    }) }) }), u(Ruler, { widthRef: this.handleInnerWidth })] }) });
  }
  // Lifecycle
  // -----------------------------------------------------------------------------------------------
  componentDidMount() {
    this._isUnmounting = false;
    this.scrollState.date = this.props.dateProfile.currentDate;
    this.scrollerRef.current.addScrollStartListener(this.handleScrollStart);
    this.scrollerRef.current.addScrollEndListener(this.handleScrollEnd);
    setTimeout(() => {
      this.applyScroll();
    }, 0);
  }
  componentDidUpdate(prevProps, prevState) {
    if (prevProps.dateProfile !== this.props.dateProfile) {
      if (this.context.options.scrollTimeReset) {
        this.resetScroll();
      } else {
        this.applyScroll();
      }
    } else if (prevState.innerWidth !== this.state.innerWidth) {
      this.applyScroll();
    }
  }
  componentWillUnmount() {
    this._isUnmounting = true;
    this.scrollerRef.current.removeScrollStartListener(this.handleScrollStart);
    this.scrollerRef.current.removeScrollEndListener(this.handleScrollEnd);
  }
  resetScroll() {
    this.scrollState.date = this.props.dateProfile.currentDate;
    this.scrollState.top = void 0;
    this.applyScroll();
  }
  applyScroll() {
    const scroller = this.scrollerRef.current;
    const top = this.computeScrollTop();
    if (scroller && top != null) {
      scroller.scrollTo({ y: top });
    }
  }
  computeScrollTop() {
    const { scrollState } = this;
    if (scrollState.top != null) {
      return scrollState.top;
    }
    if (scrollState.date != null) {
      const tilesEl = this.tilesElRef.current;
      const monthEl = tilesEl == null ? void 0 : tilesEl.querySelector(`[data-date="${formatIsoMonthStr(scrollState.date)}"]`);
      const monthWrapEl = monthEl == null ? void 0 : monthEl.parentElement;
      if (tilesEl && monthWrapEl) {
        const monthTop = Math.round(monthWrapEl.getBoundingClientRect().top);
        const originTop = Math.round(tilesEl.getBoundingClientRect().top);
        return monthTop - originTop;
      }
    }
  }
};
var oneMonthDuration = createDuration(1, "month");
function splitDateProfileByMonth(dateProfileGenerator, dateProfile, dateEnv, fixedWeekCount, showNonCurrentDates) {
  const { start, end } = dateProfile.currentRange;
  let monthStart = start;
  const monthDateProfiles = [];
  while (monthStart.valueOf() < end.valueOf()) {
    const monthEnd = dateEnv.add(monthStart, oneMonthDuration);
    const currentRange = {
      // yuck
      start: dateProfileGenerator.skipHiddenDays(monthStart),
      end: dateProfileGenerator.skipHiddenDays(monthEnd, -1, true)
    };
    let renderRange = buildDayTableRenderRange({
      currentRange,
      snapToWeek: true,
      fixedWeekCount,
      dateEnv
    });
    renderRange = {
      // yuck
      start: dateProfileGenerator.skipHiddenDays(renderRange.start),
      end: dateProfileGenerator.skipHiddenDays(renderRange.end, -1, true)
    };
    const activeRange = dateProfile.activeRange ? intersectRanges(dateProfile.activeRange, showNonCurrentDates ? renderRange : currentRange) : null;
    monthDateProfiles.push({
      currentDate: dateProfile.currentDate,
      isValid: dateProfile.isValid,
      validRange: dateProfile.validRange,
      renderRange,
      activeRange,
      currentRange,
      currentRangeUnit: "month",
      isRangeAllDay: true,
      dateIncrement: dateProfile.dateIncrement,
      slotMinTime: dateProfile.slotMaxTime,
      slotMaxTime: dateProfile.slotMinTime
    });
    monthStart = monthEnd;
  }
  return monthDateProfiles;
}
var YEAR_MONTH_FORMATTER = createFormatter({ year: "numeric", month: "long" });
var YEAR_FORMATTER = createFormatter({ month: "long" });
function buildMonthFormat(formatOverride, monthDateProfiles) {
  return formatOverride || (monthDateProfiles[0].currentRange.start.getUTCFullYear() !== monthDateProfiles[monthDateProfiles.length - 1].currentRange.start.getUTCFullYear() ? YEAR_MONTH_FORMATTER : YEAR_FORMATTER);
}
var multiMonthPlugin = {
  name: "multimonth",
  initialView: "multiMonthYear",
  views: {
    multiMonth: {
      component: MultiMonthView,
      dateProfileGeneratorClass: TableDateProfileGenerator,
      multiMonthMaxColumns: 3,
      singleMonthMinWidth: 350
    },
    multiMonthYear: {
      type: "multiMonth",
      duration: { years: 1 },
      fixedWeekCount: true,
      // TODO: apply to all multi-col layouts?
      showNonCurrentDates: false
      // TODO: looks bad when single-col layout
    }
  }
};
export {
  multiMonthPlugin as default
};
//# sourceMappingURL=@fullcalendar_vue3_multimonth.js.map
